# since functions are pieces of code intended to be reused
# multiple times, for those with only one time use: we can
# use lambdas. square = lambda x: x * x
def square(x): return x * x


print(square(9))
